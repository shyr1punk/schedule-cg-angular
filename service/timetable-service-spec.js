describe('timetableService', function() {

  beforeEach(module('schedule'));

  it('should ...', inject(function(timetableService) {

	//expect(timetableService.doSomething()).toEqual('something');

  }));

});